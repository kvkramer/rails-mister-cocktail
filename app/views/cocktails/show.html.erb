    <% if @cocktail.photo.attached? %>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @cocktail.photo.key, height: 300, width: 400, crop: :fill %>')">
    <% else %>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path "fra0v2b29elip4whik0i", width: 800, height: 300, crop: :fill %>')">
    <% end %>
<h2>🍸 <%= @cocktail.name %></h2>

</div>

<h5>🧂Ingredients: </h5>
<ul>
  <% @cocktail.doses.each do |dose| %>
    <li><%= dose.description %>
    <%= dose.ingredient.name %>
      <%= link_to "🗑 - Delete this ingredient", dose_path(dose), method: :delete, data: { confirm: 'Are you sure?' } %>
    </li>
    <% end %>

</ul>
<p><%= link_to "Add a new ingredient", new_cocktail_dose_path(@cocktail)  %></p>
<p><%= link_to "< Back", cocktails_path %></p>
